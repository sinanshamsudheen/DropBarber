import { c as customers, d as seedAppointments, f as services, l as pointEntries, m as shops, p as shopNotes, s as barbers, u as reviews } from "./states-BuolJmYz.js";
import { format, isToday, isTomorrow, parseISO } from "date-fns";
//#region src/lib/api.ts
/**
* Mock API layer — the stand-in for the real backend.
*
* Every screen talks to the product through this module only. Business rules
* that belong on the server (availability, conflicts, points, appointment
* state) are computed *here*, never in components, so this file can be swapped
* for real HTTP calls without touching the UI.
*/
var db = {
	shops: structuredClone(shops),
	services: structuredClone(services),
	barbers: structuredClone(barbers),
	customers: structuredClone(customers),
	appointments: structuredClone(seedAppointments),
	reviews: structuredClone(reviews),
	notes: structuredClone(shopNotes),
	points: structuredClone(pointEntries)
};
var CURRENT_CUSTOMER_ID = "cus-you";
var SLOT_GRID_MIN = 15;
var BUFFER_MIN = 5;
var wait = (ms = 320) => new Promise((r) => setTimeout(r, ms));
var clone = (v) => structuredClone(v);
var toMin = (t) => Number(t.slice(0, 2)) * 60 + Number(t.slice(3, 5));
var toTime = (m) => `${String(Math.floor(m / 60)).padStart(2, "0")}:${String(m % 60).padStart(2, "0")}`;
var ApiError = class extends Error {};
async function listShops(params = {}) {
	await wait();
	if (params.query?.trim().toLowerCase() === "error") throw new ApiError("We couldn't reach the search service. Check your connection and retry.");
	const q = params.query?.trim().toLowerCase() ?? "";
	const loc = params.location?.trim().toLowerCase() ?? "";
	return clone(db.shops.filter((s) => {
		const matchQ = !q || s.name.toLowerCase().includes(q) || s.area.toLowerCase().includes(q) || s.tagline.toLowerCase().includes(q);
		const matchLoc = !loc || s.area.toLowerCase().includes(loc) || s.address.toLowerCase().includes(loc);
		return matchQ && matchLoc;
	}).sort((a, b) => a.distanceKm - b.distanceKm));
}
async function getShop(shopId) {
	await wait();
	const shop = db.shops.find((s) => s.id === shopId);
	if (!shop) throw new ApiError("That shop doesn't exist or is no longer listed.");
	return {
		shop: clone(shop),
		services: clone(db.services.filter((s) => s.shopId === shopId)),
		barbers: clone(db.barbers.filter((b) => b.shopId === shopId)),
		reviews: clone(db.reviews.filter((r) => r.shopId === shopId))
	};
}
async function getBarber(shopId, barberId) {
	await wait();
	const barber = db.barbers.find((b) => b.id === barberId && b.shopId === shopId);
	const shop = db.shops.find((s) => s.id === shopId);
	if (!barber || !shop) throw new ApiError("This barber profile isn't available.");
	return {
		barber: clone(barber),
		shop: clone(shop),
		services: clone(db.services.filter((s) => s.shopId === shopId)),
		reviews: clone(db.reviews.filter((r) => r.barberId === barberId))
	};
}
/** Cheapest active service price, used for "from ₹X" on discovery cards. */
function startingPrice(shopId) {
	const prices = db.services.filter((s) => s.shopId === shopId && s.active).map((s) => s.price);
	return prices.length ? Math.min(...prices) : null;
}
async function nextAvailable(shopId) {
	const barberIds = db.barbers.filter((b) => b.shopId === shopId && b.active);
	for (let offset = 0; offset < 7; offset++) {
		const date = addDays(offset);
		for (const b of barberIds) {
			const svc = b.services.find((s) => s.active);
			if (!svc) continue;
			const first = computeSlots(b, svc.durationMin, date)[0];
			if (first) return {
				date,
				time: first.time
			};
		}
	}
	return null;
}
function addDays(offset) {
	const dt = /* @__PURE__ */ new Date();
	dt.setDate(dt.getDate() + offset);
	return dt.toISOString().slice(0, 10);
}
function computeSlots(barber, durationMin, date) {
	const day = (/* @__PURE__ */ new Date(`${date}T12:00:00`)).getDay();
	const periods = barber.schedule[day] ?? [];
	if (!periods.length) return [];
	if (barber.timeOff.some((t) => t.date === date)) return [];
	const booked = db.appointments.filter((a) => a.barberId === barber.id && a.date === date && a.status === "booked").map((a) => ({
		start: toMin(a.time),
		end: toMin(a.time) + a.durationMin + BUFFER_MIN
	}));
	const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
	const nowMin = (/* @__PURE__ */ new Date()).getHours() * 60 + (/* @__PURE__ */ new Date()).getMinutes();
	const slots = [];
	for (const p of periods) for (let t = toMin(p.start); t + durationMin <= toMin(p.end); t += SLOT_GRID_MIN) {
		if (date === today && t < nowMin + 30) continue;
		if (!booked.some((b) => t < b.end && t + durationMin > b.start)) slots.push({
			time: toTime(t),
			durationMin
		});
	}
	return slots;
}
async function getAvailability(params) {
	await wait(420);
	const barber = db.barbers.find((b) => b.id === params.barberId);
	if (!barber) throw new ApiError("Availability is unavailable for this barber right now.");
	const bs = barber.services.find((s) => s.serviceId === params.serviceId && s.active);
	if (!bs) throw new ApiError("This barber no longer offers that service.");
	return {
		slots: computeSlots(barber, bs.durationMin, params.date),
		durationMin: bs.durationMin
	};
}
async function createAppointment(input) {
	await wait(600);
	const barber = db.barbers.find((b) => b.id === input.barberId);
	const service = db.services.find((s) => s.id === input.serviceId);
	const bs = barber.services.find((s) => s.serviceId === input.serviceId);
	if (!bs) throw new ApiError("This barber no longer offers that service.");
	if (!computeSlots(barber, bs.durationMin, input.date).some((s) => s.time === input.time)) throw new ApiError("SLOT_TAKEN");
	const appointment = {
		id: `apt-${Math.random().toString(36).slice(2, 8)}`,
		shopId: input.shopId,
		barberId: input.barberId,
		serviceId: input.serviceId,
		customerId: input.customerId ?? "cus-you",
		date: input.date,
		time: input.time,
		durationMin: bs.durationMin,
		price: bs.priceOverride ?? service.price,
		status: "booked",
		note: input.note,
		referencePhotos: input.referencePhotos,
		createdAt: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)
	};
	db.appointments.push(appointment);
	return clone(appointment);
}
function hydrate(a) {
	return {
		...clone(a),
		shop: clone(db.shops.find((s) => s.id === a.shopId)),
		barber: clone(db.barbers.find((b) => b.id === a.barberId)),
		service: clone(db.services.find((s) => s.id === a.serviceId)),
		customer: clone(db.customers.find((c) => c.id === a.customerId)),
		review: clone(db.reviews.find((r) => r.appointmentId === a.id) ?? null)
	};
}
var startsAt = (a) => (/* @__PURE__ */ new Date(`${a.date}T${a.time}:00`)).getTime();
async function listCustomerAppointments(customerId = CURRENT_CUSTOMER_ID) {
	await wait();
	const all = db.appointments.filter((a) => a.customerId === customerId).map(hydrate);
	const now = Date.now();
	return {
		upcoming: all.filter((a) => a.status === "booked" && startsAt(a) >= now - 36e5).sort((a, b) => startsAt(a) - startsAt(b)),
		past: all.filter((a) => !(a.status === "booked" && startsAt(a) >= now - 36e5)).sort((a, b) => startsAt(b) - startsAt(a))
	};
}
async function getAppointment(id) {
	await wait();
	const a = db.appointments.find((x) => x.id === id);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	return hydrate(a);
}
async function cancelAppointment(id) {
	await wait(400);
	const a = db.appointments.find((x) => x.id === id);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	if (a.status !== "booked") throw new ApiError("This appointment can no longer be cancelled.");
	a.status = "cancelled";
	return hydrate(a);
}
async function listShopAppointments(shopId, date) {
	await wait();
	return db.appointments.filter((a) => a.shopId === shopId && a.date === date).map(hydrate).sort((a, b) => a.time.localeCompare(b.time));
}
async function getShopDay(shopId) {
	await wait();
	const date = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
	const appointments = db.appointments.filter((a) => a.shopId === shopId && a.date === date).map(hydrate).sort((a, b) => a.time.localeCompare(b.time));
	const shopBarbers = db.barbers.filter((b) => b.shopId === shopId);
	const day = (/* @__PURE__ */ new Date(`${date}T12:00:00`)).getDay();
	return {
		date,
		appointments,
		barbers: shopBarbers.map((b) => ({
			id: b.id,
			name: b.name,
			active: b.active,
			working: (b.schedule[day] ?? []).length > 0 && !b.timeOff.some((t) => t.date === date),
			periods: b.schedule[day] ?? [],
			todayCount: appointments.filter((a) => a.barberId === b.id).length
		}))
	};
}
async function completeAppointment(id, data) {
	await wait(500);
	const a = db.appointments.find((x) => x.id === id);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	a.status = "completed";
	a.completion = {
		...data,
		pointsAwarded: 10
	};
	const barber = db.barbers.find((b) => b.id === a.barberId);
	if (barber) {
		barber.points += 10;
		db.points.unshift({
			id: `pt-${Math.random().toString(36).slice(2, 7)}`,
			barberId: barber.id,
			points: 10,
			reason: `Completed record — ${db.customers.find((c) => c.id === a.customerId)?.name ?? "Customer"}`,
			date: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)
		});
	}
	if (data.notes) db.notes.unshift({
		id: `note-${Math.random().toString(36).slice(2, 7)}`,
		shopId: a.shopId,
		customerId: a.customerId,
		text: data.notes,
		date: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
		author: barber?.name ?? "Staff"
	});
	return hydrate(a);
}
async function closeAppointmentWithoutDetails(id) {
	await wait(350);
	const a = db.appointments.find((x) => x.id === id);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	a.status = "completed";
	return hydrate(a);
}
async function markNoShow(id) {
	await wait(350);
	const a = db.appointments.find((x) => x.id === id);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	a.status = "no_show";
	return hydrate(a);
}
async function listShopCustomers(shopId, query = "") {
	await wait();
	const q = query.trim().toLowerCase();
	return [...new Set(db.appointments.filter((a) => a.shopId === shopId).map((a) => a.customerId))].map((id) => {
		const customer = db.customers.find((c) => c.id === id);
		const visits = db.appointments.filter((a) => a.customerId === id && a.shopId === shopId && a.status === "completed");
		const all = db.appointments.filter((a) => a.customerId === id && a.shopId === shopId).sort((x, y) => startsAt(y) - startsAt(x));
		const counts = /* @__PURE__ */ new Map();
		all.forEach((a) => counts.set(a.barberId, (counts.get(a.barberId) ?? 0) + 1));
		const preferredId = [...counts.entries()].sort((a, b) => b[1] - a[1])[0]?.[0];
		return {
			customer: clone(customer),
			visits: visits.length,
			lastVisit: visits[0]?.date ?? all.find((a) => a.status === "completed")?.date ?? null,
			lastAppointmentDate: all[0]?.date ?? null,
			preferredBarber: preferredId ? clone(db.barbers.find((b) => b.id === preferredId)) : null,
			spend: visits.reduce((sum, a) => sum + (a.completion?.finalPrice ?? a.price), 0)
		};
	}).filter((row) => !q || row.customer.name.toLowerCase().includes(q) || row.customer.phone.includes(q) || row.customer.email.toLowerCase().includes(q)).sort((a, b) => (b.lastAppointmentDate ?? "").localeCompare(a.lastAppointmentDate ?? ""));
}
async function getShopCustomer(shopId, customerId) {
	await wait();
	const customer = db.customers.find((c) => c.id === customerId);
	if (!customer) throw new ApiError("We couldn't find that customer.");
	/** PRIVACY: only appointments belonging to this shop are ever returned. */
	const appointments = db.appointments.filter((a) => a.customerId === customerId && a.shopId === shopId).map(hydrate).sort((a, b) => startsAt(b) - startsAt(a));
	const completed = appointments.filter((a) => a.status === "completed");
	const counts = /* @__PURE__ */ new Map();
	appointments.forEach((a) => counts.set(a.barberId, (counts.get(a.barberId) ?? 0) + 1));
	const preferredId = [...counts.entries()].sort((a, b) => b[1] - a[1])[0]?.[0];
	return {
		customer: clone(customer),
		appointments,
		visits: completed.length,
		lastVisit: completed[0]?.date ?? null,
		spend: completed.reduce((s, a) => s + (a.completion?.finalPrice ?? a.price), 0),
		preferredBarber: preferredId ? clone(db.barbers.find((b) => b.id === preferredId)) : null,
		notes: clone(db.notes.filter((n) => n.shopId === shopId && n.customerId === customerId)),
		referencePhotos: appointments.flatMap((a) => a.referencePhotos)
	};
}
async function addShopNote(shopId, customerId, text, author) {
	await wait(300);
	const note = {
		id: `note-${Math.random().toString(36).slice(2, 7)}`,
		shopId,
		customerId,
		text,
		author,
		date: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)
	};
	db.notes.unshift(note);
	return clone(note);
}
async function listBarbers(shopId) {
	await wait();
	const today = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
	return db.barbers.filter((b) => b.shopId === shopId).map((b) => ({
		barber: clone(b),
		todayCount: db.appointments.filter((a) => a.barberId === b.id && a.date === today && a.status !== "cancelled").length
	}));
}
async function getManagedBarber(shopId, barberId) {
	await wait();
	const barber = db.barbers.find((b) => b.id === barberId && b.shopId === shopId);
	if (!barber) throw new ApiError("We couldn't find that barber.");
	return {
		barber: clone(barber),
		services: clone(db.services.filter((s) => s.shopId === shopId))
	};
}
async function saveBarber(shopId, input) {
	await wait(450);
	if (input.id) {
		const b = db.barbers.find((x) => x.id === input.id);
		Object.assign(b, input);
		return clone(b);
	}
	const barber = {
		id: `brb-${Math.random().toString(36).slice(2, 7)}`,
		shopId,
		name: input.name ?? "New barber",
		bio: input.bio ?? "",
		active: true,
		rating: 0,
		reviewCount: 0,
		points: 0,
		services: input.services ?? [],
		schedule: input.schedule ?? [
			[],
			[],
			[],
			[],
			[],
			[],
			[]
		],
		timeOff: []
	};
	db.barbers.push(barber);
	return clone(barber);
}
async function setBarberActive(barberId, active) {
	await wait(250);
	const b = db.barbers.find((x) => x.id === barberId);
	b.active = active;
	return clone(b);
}
async function setBarberService(barberId, serviceId, patch) {
	await wait(250);
	const b = db.barbers.find((x) => x.id === barberId);
	const existing = b.services.find((s) => s.serviceId === serviceId);
	if (existing) Object.assign(existing, patch);
	else b.services.push({
		serviceId,
		durationMin: patch.durationMin ?? 20,
		active: patch.active ?? true,
		priceOverride: patch.priceOverride
	});
	return clone(b);
}
async function setBarberSchedule(barberId, schedule) {
	await wait(350);
	const b = db.barbers.find((x) => x.id === barberId);
	b.schedule = schedule;
	return clone(b);
}
async function addTimeOff(barberId, date, reason) {
	await wait(300);
	const b = db.barbers.find((x) => x.id === barberId);
	b.timeOff.push({
		id: `to-${Math.random().toString(36).slice(2, 7)}`,
		date,
		reason
	});
	return clone(b);
}
async function removeTimeOff(barberId, id) {
	await wait(250);
	const b = db.barbers.find((x) => x.id === barberId);
	b.timeOff = b.timeOff.filter((t) => t.id !== id);
	return clone(b);
}
async function listServices(shopId) {
	await wait();
	return clone(db.services.filter((s) => s.shopId === shopId));
}
async function saveService(shopId, input) {
	await wait(400);
	if (input.id) {
		const s = db.services.find((x) => x.id === input.id);
		Object.assign(s, input);
		return clone(s);
	}
	const service = {
		id: `svc-${Math.random().toString(36).slice(2, 7)}`,
		shopId,
		name: input.name ?? "New service",
		description: input.description ?? "",
		price: input.price ?? 0,
		active: true
	};
	db.services.push(service);
	return clone(service);
}
async function listShopReviews(shopId) {
	await wait();
	const list = db.reviews.filter((r) => r.shopId === shopId);
	const distribution = [
		5,
		4,
		3,
		2,
		1
	].map((star) => ({
		star,
		count: list.filter((r) => Math.round(r.shopRating) === star).length
	}));
	const average = list.length ? list.reduce((s, r) => s + r.shopRating, 0) / list.length : 0;
	return {
		reviews: clone(list),
		distribution,
		average,
		total: list.length
	};
}
async function submitReview(input) {
	await wait(500);
	const a = db.appointments.find((x) => x.id === input.appointmentId);
	if (!a) throw new ApiError("We couldn't find that appointment.");
	if (a.status !== "completed") throw new ApiError("You can only review completed appointments.");
	const customer = db.customers.find((c) => c.id === a.customerId);
	const review = {
		id: `rev-${Math.random().toString(36).slice(2, 7)}`,
		shopId: a.shopId,
		barberId: a.barberId,
		appointmentId: a.id,
		customerId: a.customerId,
		customerName: `${customer.name.split(" ")[0]} ${customer.name.split(" ")[1]?.[0] ?? ""}.`,
		shopRating: input.shopRating,
		barberRating: input.barberRating,
		text: input.text,
		date: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10)
	};
	db.reviews.unshift(review);
	return clone(review);
}
async function getBarberPoints(shopId) {
	await wait();
	return db.barbers.filter((b) => b.shopId === shopId).map((b) => ({
		barber: clone(b),
		history: clone(db.points.filter((p) => p.barberId === b.id))
	})).sort((a, b) => b.barber.points - a.barber.points);
}
async function getCustomerProfile(customerId = CURRENT_CUSTOMER_ID) {
	await wait();
	return clone(db.customers.find((c) => c.id === customerId));
}
async function saveCustomerProfile(customerId, patch) {
	await wait(350);
	const c = db.customers.find((x) => x.id === customerId);
	Object.assign(c, patch);
	return clone(c);
}
async function getCustomerHistory(customerId = CURRENT_CUSTOMER_ID) {
	await wait();
	/** The customer's own cross-shop history. Shops never receive this view. */
	return db.appointments.filter((a) => a.customerId === customerId && a.status !== "booked").map(hydrate).sort((a, b) => startsAt(b) - startsAt(a));
}
async function updateShopSettings(shopId, patch) {
	await wait(450);
	const s = db.shops.find((x) => x.id === shopId);
	Object.assign(s, patch);
	return clone(s);
}
//#endregion
//#region src/lib/format.ts
var money = (v) => `₹${v.toLocaleString("en-IN")}`;
var duration = (min) => min >= 60 ? `${Math.floor(min / 60)}h${min % 60 ? ` ${min % 60}m` : ""}` : `${min} min`;
function dayLabel(date) {
	const d = parseISO(date);
	if (isToday(d)) return "Today";
	if (isTomorrow(d)) return "Tomorrow";
	return format(d, "EEE d MMM");
}
function longDate(date) {
	return format(parseISO(date), "EEEE d MMMM yyyy");
}
function timeLabel(time) {
	const [h = 0, m = 0] = time.split(":").map(Number);
	const suffix = h >= 12 ? "PM" : "AM";
	return `${h % 12 === 0 ? 12 : h % 12}:${String(m).padStart(2, "0")} ${suffix}`;
}
var todayISO = () => (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
function addDaysISO(offset, from = /* @__PURE__ */ new Date()) {
	const d = new Date(from);
	d.setDate(d.getDate() + offset);
	return d.toISOString().slice(0, 10);
}
var DAY_NAMES = [
	"Sunday",
	"Monday",
	"Tuesday",
	"Wednesday",
	"Thursday",
	"Friday",
	"Saturday"
];
//#endregion
export { listShopCustomers as A, setBarberSchedule as B, getShop as C, listCustomerAppointments as D, listBarbers as E, removeTimeOff as F, startingPrice as H, saveBarber as I, saveCustomerProfile as L, listShops as M, markNoShow as N, listServices as O, nextAvailable as P, saveService as R, getManagedBarber as S, getShopDay as T, submitReview as U, setBarberService as V, updateShopSettings as W, getAvailability as _, longDate as a, getCustomerHistory as b, todayISO as c, addTimeOff as d, cancelAppointment as f, getAppointment as g, createAppointment as h, duration as i, listShopReviews as j, listShopAppointments as k, CURRENT_CUSTOMER_ID as l, completeAppointment as m, addDaysISO as n, money as o, closeAppointmentWithoutDetails as p, dayLabel as r, timeLabel as s, DAY_NAMES as t, addShopNote as u, getBarber as v, getShopCustomer as w, getCustomerProfile as x, getBarberPoints as y, setBarberActive as z };
