import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
//#region src/routes/manage.$shopId.appointments.index.tsx
var $$splitComponentImporter = () => import("./manage._shopId.appointments.index-Cm-SXFj9.js");
var Route = createFileRoute("/manage/$shopId/appointments/")({
	validateSearch: (search) => {
		const out = {};
		if (typeof search["date"] === "string") out.date = search["date"];
		if (typeof search["barberId"] === "string") out.barberId = search["barberId"];
		if (typeof search["status"] === "string") out.status = search["status"];
		return out;
	},
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
