import { r as cn } from "./button-PwNqyxv_.js";
import { Link, useRouterState } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { CalendarCheck, Clock3, Compass, User } from "lucide-react";
//#region src/components/layout/customer-shell.tsx
var NAV = [
	{
		to: "/",
		label: "Discover",
		icon: Compass,
		match: (p) => p === "/" || p.startsWith("/shops")
	},
	{
		to: "/bookings",
		label: "Bookings",
		icon: CalendarCheck,
		match: (p) => p.startsWith("/bookings")
	},
	{
		to: "/history",
		label: "History",
		icon: Clock3,
		match: (p) => p.startsWith("/history")
	},
	{
		to: "/profile",
		label: "Profile",
		icon: User,
		match: (p) => p.startsWith("/profile")
	}
];
function MobileBottomNav() {
	const pathname = useRouterState({ select: (s) => s.location.pathname });
	return /* @__PURE__ */ jsx("nav", {
		"aria-label": "Main",
		className: "fixed inset-x-0 bottom-0 z-40 border-t border-border bg-background/95 pb-[env(safe-area-inset-bottom)] backdrop-blur",
		children: /* @__PURE__ */ jsx("ul", {
			className: "mx-auto flex max-w-2xl",
			children: NAV.map((item) => {
				const active = item.match(pathname);
				return /* @__PURE__ */ jsx("li", {
					className: "flex-1",
					children: /* @__PURE__ */ jsxs(Link, {
						to: item.to,
						"aria-current": active ? "page" : void 0,
						className: cn("flex min-h-14 flex-col items-center justify-center gap-0.5 py-2 text-[11px] font-medium transition-colors", active ? "text-accent" : "text-muted-foreground hover:text-foreground"),
						children: [/* @__PURE__ */ jsx(item.icon, {
							className: "size-5",
							"aria-hidden": true
						}), item.label]
					})
				}, item.to);
			})
		})
	});
}
function CustomerShell({ children, hideNav, className }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-background",
		children: [/* @__PURE__ */ jsx("main", {
			className: cn("pb-24", className),
			children
		}), !hideNav && /* @__PURE__ */ jsx(MobileBottomNav, {})]
	});
}
function PageHeader({ title, description, action }) {
	return /* @__PURE__ */ jsxs("header", {
		className: "grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3 py-5",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "min-w-0",
			children: [/* @__PURE__ */ jsx("h1", {
				className: "truncate text-2xl font-semibold",
				children: title
			}), description && /* @__PURE__ */ jsx("p", {
				className: "mt-1 text-sm text-muted-foreground",
				children: description
			})]
		}), action]
	});
}
//#endregion
export { PageHeader as n, CustomerShell as t };
