import { t as Route } from "./manage._shopId.customers.index-GbPZWN3q.js";
import { t as Input } from "./input-uzm9g8Y7.js";
import { a as ListSkeleton, n as EmptyState, r as ErrorState } from "./states-BuolJmYz.js";
import { A as listShopCustomers, o as money, r as dayLabel } from "./format-CnmWxffg.js";
import { t as ManageHeader } from "./manage-shell-JKDUkZgW.js";
import { a as AvatarFallback, i as Avatar, o as AvatarImage, r as initials } from "./barber-card-K0LXgGTr.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { ChevronRight, Search, Users } from "lucide-react";
//#region src/components/cards/customer-card.tsx
function CustomerCard({ shopId, row }) {
	const { customer, visits, lastVisit, preferredBarber, spend } = row;
	return /* @__PURE__ */ jsxs(Link, {
		to: "/manage/$shopId/customers/$customerId",
		params: {
			shopId,
			customerId: customer.id
		},
		className: "flex items-center gap-3 rounded-2xl border border-border bg-card p-4 transition-colors hover:border-accent/50",
		children: [
			/* @__PURE__ */ jsxs(Avatar, {
				className: "size-11 shrink-0",
				children: [customer.photo && /* @__PURE__ */ jsx(AvatarImage, {
					src: customer.photo,
					alt: ""
				}), /* @__PURE__ */ jsx(AvatarFallback, {
					className: "bg-secondary font-display",
					children: initials(customer.name)
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "min-w-0 flex-1",
				children: [
					/* @__PURE__ */ jsx("p", {
						className: "truncate text-sm font-semibold",
						children: customer.name
					}),
					/* @__PURE__ */ jsxs("p", {
						className: "truncate text-xs text-muted-foreground",
						children: [
							visits,
							" visit",
							visits === 1 ? "" : "s",
							lastVisit ? ` · last ${dayLabel(lastVisit)}` : " · no completed visits yet",
							spend > 0 ? ` · ${money(spend)}` : ""
						]
					}),
					preferredBarber && /* @__PURE__ */ jsxs("p", {
						className: "truncate text-xs text-muted-foreground",
						children: ["Usually sees ", preferredBarber.name]
					})
				]
			}),
			/* @__PURE__ */ jsx(ChevronRight, {
				className: "size-4 shrink-0 text-muted-foreground",
				"aria-hidden": true
			})
		]
	});
}
//#endregion
//#region src/routes/manage.$shopId.customers.index.tsx?tsr-split=component
function CustomersPage() {
	const { shopId } = Route.useParams();
	const [query, setQuery] = useState("");
	const q = useQuery({
		queryKey: [
			"shop-customers",
			shopId,
			query
		],
		queryFn: () => listShopCustomers(shopId, query)
	});
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(ManageHeader, {
			title: "Customers",
			description: "Only visits to this shop are shown — never a customer's history elsewhere."
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "relative",
			children: [/* @__PURE__ */ jsx(Search, {
				className: "pointer-events-none absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground",
				"aria-hidden": true
			}), /* @__PURE__ */ jsx(Input, {
				value: query,
				onChange: (e) => setQuery(e.target.value),
				placeholder: "Search name, phone or email",
				className: "h-11 pl-9",
				"aria-label": "Search customers"
			})]
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mt-4",
			children: [
				q.isPending && /* @__PURE__ */ jsx(ListSkeleton, { rows: 4 }),
				q.isError && /* @__PURE__ */ jsx(ErrorState, {
					message: q.error.message,
					onRetry: () => void q.refetch()
				}),
				q.isSuccess && q.data.length === 0 && /* @__PURE__ */ jsx(EmptyState, {
					icon: Users,
					title: query ? "No customers match that search" : "No customers yet",
					description: query ? "Try a different name or phone number." : "Customers appear here after their first booking with you."
				}),
				/* @__PURE__ */ jsx("ul", {
					className: "space-y-2",
					children: q.data?.map((row) => /* @__PURE__ */ jsx("li", { children: /* @__PURE__ */ jsx(CustomerCard, {
						shopId,
						row
					}) }, row.customer.id))
				})
			]
		})
	] });
}
//#endregion
export { CustomersPage as component };
