import { t as Button } from "./button-PwNqyxv_.js";
import { t as Label } from "./label-BeT0bXvu.js";
import { n as StarInput } from "./rating-CKR3gSkJ.js";
import { a as DialogHeader, i as DialogFooter, n as DialogContent, o as DialogTitle, r as DialogDescription, s as DialogTrigger, t as Dialog } from "./dialog-BvYONHWJ.js";
import { t as Textarea } from "./textarea-DjqHhWkA.js";
import { U as submitReview } from "./format-CnmWxffg.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
//#region src/components/booking/review-dialog.tsx
function ReviewDialog({ appointmentId, shopName, barberName, trigger }) {
	const [open, setOpen] = useState(false);
	const [shopRating, setShopRating] = useState(0);
	const [barberRating, setBarberRating] = useState(0);
	const [text, setText] = useState("");
	const [error, setError] = useState(null);
	const queryClient = useQueryClient();
	const mutation = useMutation({
		mutationFn: () => submitReview({
			appointmentId,
			shopRating,
			...barberRating ? { barberRating } : {},
			text: text.trim()
		}),
		onSuccess: () => {
			toast.success("Thanks for the review", { description: "It's now visible on the shop's profile." });
			queryClient.invalidateQueries();
			setOpen(false);
		},
		onError: (e) => setError(e.message)
	});
	return /* @__PURE__ */ jsxs(Dialog, {
		open,
		onOpenChange: setOpen,
		children: [/* @__PURE__ */ jsx(DialogTrigger, {
			asChild: true,
			children: trigger
		}), /* @__PURE__ */ jsxs(DialogContent, {
			className: "max-w-md",
			children: [
				/* @__PURE__ */ jsxs(DialogHeader, { children: [/* @__PURE__ */ jsx(DialogTitle, { children: "Review your visit" }), /* @__PURE__ */ jsxs(DialogDescription, { children: [
					"Reviews are tied to a real completed appointment at ",
					shopName,
					"."
				] })] }),
				/* @__PURE__ */ jsxs("div", {
					className: "space-y-5",
					children: [
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, { children: "How was the shop?" }), /* @__PURE__ */ jsx(StarInput, {
							value: shopRating,
							onChange: setShopRating,
							label: "Shop rating"
						})] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsxs(Label, { children: [
							"How was ",
							barberName,
							"? (optional)"
						] }), /* @__PURE__ */ jsx(StarInput, {
							value: barberRating,
							onChange: setBarberRating,
							label: "Barber rating"
						})] }),
						/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx(Label, {
							htmlFor: "review-text",
							children: "Your review"
						}), /* @__PURE__ */ jsx(Textarea, {
							id: "review-text",
							className: "mt-1.5 min-h-24",
							value: text,
							onChange: (e) => setText(e.target.value),
							placeholder: "What stood out?"
						})] }),
						error && /* @__PURE__ */ jsx("p", {
							role: "alert",
							className: "text-sm text-destructive",
							children: error
						})
					]
				}),
				/* @__PURE__ */ jsx(DialogFooter, { children: /* @__PURE__ */ jsx(Button, {
					className: "h-11 w-full",
					disabled: !shopRating || mutation.isPending,
					onClick: () => {
						setError(null);
						if (!shopRating) {
							setError("Please give the shop a star rating.");
							return;
						}
						mutation.mutate();
					},
					children: mutation.isPending ? "Submitting…" : "Submit review"
				}) })
			]
		})]
	});
}
//#endregion
export { ReviewDialog as t };
