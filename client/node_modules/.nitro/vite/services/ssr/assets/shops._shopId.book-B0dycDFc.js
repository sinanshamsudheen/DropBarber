import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
//#region src/routes/shops.$shopId.book.tsx
var $$splitComponentImporter = () => import("./shops._shopId.book-CscPXXt6.js");
var Route = createFileRoute("/shops/$shopId/book")({
	validateSearch: (search) => {
		const out = { step: Number(search["step"]) || 1 };
		if (typeof search["serviceId"] === "string") out.serviceId = search["serviceId"];
		if (typeof search["barberId"] === "string") out.barberId = search["barberId"];
		if (typeof search["date"] === "string") out.date = search["date"];
		if (typeof search["time"] === "string") out.time = search["time"];
		return out;
	},
	head: () => ({ meta: [
		{ title: "Book an appointment — Drop" },
		{
			name: "description",
			content: "Pick a service, barber, date and an available time slot."
		},
		{
			property: "og:title",
			content: "Book an appointment — Drop"
		},
		{
			property: "og:description",
			content: "Pick a service, barber, date and time in a few taps."
		}
	] }),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
