import { r as cn } from "./button-PwNqyxv_.js";
import { n as addDaysISO } from "./format-CnmWxffg.js";
import { jsx, jsxs } from "react/jsx-runtime";
import { format, parseISO } from "date-fns";
//#region src/components/booking/date-strip.tsx
function DateStrip({ value, onChange, days = 14 }) {
	return /* @__PURE__ */ jsx("div", {
		className: "-mx-4 overflow-x-auto px-4 pb-1",
		children: /* @__PURE__ */ jsx("div", {
			role: "radiogroup",
			"aria-label": "Choose a date",
			className: "flex gap-2",
			children: Array.from({ length: days }, (_, i) => addDaysISO(i)).map((d) => {
				const parsed = parseISO(d);
				const selected = d === value;
				return /* @__PURE__ */ jsxs("button", {
					type: "button",
					role: "radio",
					"aria-checked": selected,
					onClick: () => onChange(d),
					className: cn("flex min-h-16 w-16 shrink-0 flex-col items-center justify-center rounded-xl border text-sm transition-colors", selected ? "border-accent bg-accent text-accent-foreground" : "border-border bg-card hover:border-accent/50"),
					children: [
						/* @__PURE__ */ jsx("span", {
							className: "text-[11px] uppercase tracking-wide opacity-80",
							children: format(parsed, "EEE")
						}),
						/* @__PURE__ */ jsx("span", {
							className: "text-lg font-semibold leading-tight",
							children: format(parsed, "d")
						}),
						/* @__PURE__ */ jsx("span", {
							className: "text-[11px] opacity-80",
							children: format(parsed, "MMM")
						})
					]
				}, d);
			})
		})
	});
}
//#endregion
export { DateStrip as t };
